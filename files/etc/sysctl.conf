# === Gaming Network Optimization for OpenWrt ===
# Applied at every boot via sysctl.d / sysctl -p

# --- TCP Congestion Control ---
net.ipv4.tcp_congestion_control = bbr
net.ipv4.tcp_slow_start_after_idle = 0

# --- TCP Low-Latency Tuning ---
net.ipv4.tcp_timestamps = 0
net.ipv4.tcp_fastopen = 3
net.ipv4.tcp_notsent_lowat = 131072
net.ipv4.tcp_keepalive_time = 60
net.ipv4.tcp_wmem = 4096 32768 4194304

# --- UDP Buffer Tuning (gaming) ---
net.core.rmem_default = 262144
net.core.wmem_default = 262144

# --- Conntrack UDP Timeout (fast stale cleanup) ---
net.netfilter.nf_conntrack_udp_timeout = 30
net.netfilter.nf_conntrack_udp_timeout_stream = 60

# --- NAPI / Packet Processing ---
net.core.netdev_budget = 600

# --- RPS Flow Steering ---
net.core.rps_sock_flow_entries = 32768

# --- Socket Backlog ---
net.core.somaxconn = 8192

# --- IPv6: disabled (ISP不支持 IPv6-PD over PPPoE) ---
# 若以后 ISP 支持，删掉下面两行即可启用双栈
net.ipv6.conf.all.disable_ipv6 = 1
net.ipv6.conf.default.disable_ipv6 = 1
