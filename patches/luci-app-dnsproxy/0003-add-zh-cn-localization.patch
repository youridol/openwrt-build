diff --git a/www/luci-static/resources/tools/dnsproxy/settings.js b/www/luci-static/resources/tools/dnsproxy/settings.js
index 7f221d2..86c1396 100644
--- a/www/luci-static/resources/tools/dnsproxy/settings.js
+++ b/www/luci-static/resources/tools/dnsproxy/settings.js
@@ -6,61 +6,61 @@ return L.Class.extend({
   addSection: function(m) {
     var s, o;
 
-    s = m.section(form.NamedSection, 'settings', _('Settings'));
-    s.tab('general', _('General'));
-    s.tab('servers', _('Servers'));
-    s.tab('cache',   _('Cache'));
+    s = m.section(form.NamedSection, 'settings', _('设置'));
+    s.tab('general', _('常规'));
+    s.tab('servers', _('服务器'));
+    s.tab('cache',   _('缓存'));
     s.tab('tls',     _('TLS / HTTPS / QUIC'));
-    s.tab('privacy', _('Privacy & Security'));
-    s.tab('perf',    _('Performance'));
+    s.tab('privacy', _('隐私与安全'));
+    s.tab('perf',    _('性能'));
 
     /* ── General ── */
     o = s.taboption('general', form.DynamicList, 'listen_addr',
-      _('Listen addresses'), _('Example: 0.0.0.0, ::1'));
+      _('监听地址'), _('示例：0.0.0.0, ::1'));
     o.placeholder = '0.0.0.0';
     o.cfgvalue = function() { return uci.get('dnsproxy', 'global', 'listen_addr'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'global', 'listen_addr', val); };
 
-    o = s.taboption('general', form.DynamicList, 'listen_port', _('Listen ports'));
+    o = s.taboption('general', form.DynamicList, 'listen_port', _('监听端口'));
     o.placeholder = '53';
     o.datatype = 'port';
     o.cfgvalue = function() { return uci.get('dnsproxy', 'global', 'listen_port'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'global', 'listen_port', val); };
 
-    o = s.taboption('general', form.ListValue, 'upstream_mode', _('Upstream mode'),
-      _('load_balance: round-robin. parallel: query all. fastest_addr: return fastest IP.'));
-    o.value('',             _('load_balance (default)'));
+    o = s.taboption('general', form.ListValue, 'upstream_mode', _('上游模式'),
+      _('load_balance：轮询。parallel：并行查询全部。fastest_addr：返回最快 IP。'));
+    o.value('',             _('负载均衡（默认）'));
     o.value('load_balance', 'load_balance');
     o.value('parallel',     'parallel');
     o.value('fastest_addr', 'fastest_addr');
     o.cfgvalue = function() { return uci.get('dnsproxy', 'global', 'upstream_mode'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'global', 'upstream_mode', val); };
 
-    o = s.taboption('general', form.Flag, 'ipv6_disabled', _('Disable IPv6'),
-      _('Reply NOERROR with empty answer to all AAAA requests'));
+    o = s.taboption('general', form.Flag, 'ipv6_disabled', _('禁用 IPv6'),
+      _('对所有 AAAA 请求回复空应答的 NOERROR'));
     o.rmempty = false;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'global', 'ipv6_disabled'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'global', 'ipv6_disabled', val); };
 
-    o = s.taboption('general', form.Flag, 'http3', _('Enable HTTP/3'),
-      _('Use HTTP/3 for DoH upstreams if available'));
+    o = s.taboption('general', form.Flag, 'http3', _('启用 HTTP/3'),
+      _('如可用，对 DoH 上游使用 HTTP/3'));
     o.rmempty = false;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'global', 'http3'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'global', 'http3', val); };
 
     o = s.taboption('general', form.Flag, 'insecure',
-      _('Disable TLS certificate validation'), _('Use only for testing — insecure!'));
+      _('禁用 TLS 证书校验'), _('仅用于测试——不安全！'));
     o.rmempty = false;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'global', 'insecure'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'global', 'insecure', val); };
 
-    o = s.taboption('general', form.Flag, 'verbose', _('Verbose logging'));
+    o = s.taboption('general', form.Flag, 'verbose', _('详细日志'));
     o.rmempty = false;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'global', 'verbose'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'global', 'verbose', val); };
 
-    o = s.taboption('general', form.Value, 'log_file', _('Log file path'),
-      _('Leave empty to log to stdout'));
+    o = s.taboption('general', form.Value, 'log_file', _('日志文件路径'),
+      _('留空则输出到标准输出'));
     o.placeholder = '/var/log/dnsproxy.log';
     o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'global', 'log_file'); };
@@ -68,249 +68,249 @@ return L.Class.extend({
 
     /* ── ALL_DOH: LAN DNS 强制拦截（本地 patch 新增，勿在上游修改）── */
     o = s.taboption('general', form.Flag, 'lan_dns_intercept',
-      '拦截所有客户端 DNS（Intercept all client DNS）',
+      '拦截所有客户端 DNS',
       '将所有 LAN 客户端发往任意地址的 DNS 查询（UDP/TCP 端口 53）强制重定向到本机 dnsproxy 链路，' +
       '客户端手动指定的 DNS（如 8.8.8.8 / 1.1.1.1）无法绕过本路由器。' +
-      '通过 dnsproxy init + firewall4(/etc/nftables.d) 自动管理，UCI 键: dnsproxy.global.lan_dns_intercept');
+      '由 dnsproxy init 自动管理（fw3 用 iptables、fw4 用 nftables），UCI 键: dnsproxy.global.lan_dns_intercept');
     o.rmempty = false;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'global', 'lan_dns_intercept'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'global', 'lan_dns_intercept', val); };
 
     /* ── Servers ── */
-    o = s.taboption('servers', form.DynamicList, 'upstream', _('Upstream servers'),
-      _('Supports plain DNS, DoT (tls://), DoH (https://), DoQ (quic://), DNSCrypt (sdns://)'));
+    o = s.taboption('servers', form.DynamicList, 'upstream', _('上游服务器'),
+      _('支持明文 DNS、DoT (tls://)、DoH (https://)、DoQ (quic://)、DNSCrypt (sdns://)'));
     o.placeholder = 'tls://1.1.1.1';
     o.cfgvalue = function() { return uci.get('dnsproxy', 'servers', 'upstream'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'servers', 'upstream', val); };
 
-    o = s.taboption('servers', form.DynamicList, 'bootstrap', _('Bootstrap servers'),
-      _('Plain DNS servers to resolve DoH/DoT hostnames'));
+    o = s.taboption('servers', form.DynamicList, 'bootstrap', _('Bootstrap 服务器'),
+      _('用于解析 DoH/DoT 主机名的明文 DNS 服务器'));
     o.placeholder = '8.8.8.8';
     o.cfgvalue = function() { return uci.get('dnsproxy', 'servers', 'bootstrap'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'servers', 'bootstrap', val); };
 
-    o = s.taboption('servers', form.DynamicList, 'fallback', _('Fallback servers'),
-      _('Used when upstream is unavailable'));
+    o = s.taboption('servers', form.DynamicList, 'fallback', _('备用服务器'),
+      _('上游不可用时使用'));
     o.placeholder = 'tls://9.9.9.9';
     o.cfgvalue = function() { return uci.get('dnsproxy', 'servers', 'fallback'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'servers', 'fallback', val); };
 
     /* ── Cache ── */
-    o = s.taboption('cache', form.Flag, 'cache_enabled', _('Enable DNS cache'));
+    o = s.taboption('cache', form.Flag, 'cache_enabled', _('启用 DNS 缓存'));
     o.rmempty = false;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'cache', 'enabled'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'cache', 'enabled', val); };
 
-    o = s.taboption('cache', form.Flag, 'cache_optimistic', _('Optimistic cache'),
-      _('Return expired entries immediately and refresh in background'));
+    o = s.taboption('cache', form.Flag, 'cache_optimistic', _('乐观缓存'),
+      _('立即返回过期条目并在后台刷新'));
     o.rmempty = false;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'cache', 'cache_optimistic'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'cache', 'cache_optimistic', val); };
 
-    o = s.taboption('cache', form.Value, 'cache_size', _('Cache size (bytes)'));
+    o = s.taboption('cache', form.Value, 'cache_size', _('缓存大小（字节）'));
     o.placeholder = '65535'; o.datatype = 'uinteger';
     o.cfgvalue = function() { return uci.get('dnsproxy', 'cache', 'size'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'cache', 'size', val); };
 
-    o = s.taboption('cache', form.Value, 'cache_min_ttl', _('Minimum TTL (seconds)'),
-      _('Capped at 3600. Use with care.'));
+    o = s.taboption('cache', form.Value, 'cache_min_ttl', _('最小 TTL（秒）'),
+      _('上限 3600。请谨慎使用。'));
     o.datatype = 'uinteger'; o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'cache', 'min_ttl'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'cache', 'min_ttl', val); };
 
-    o = s.taboption('cache', form.Value, 'cache_max_ttl', _('Maximum TTL (seconds)'));
+    o = s.taboption('cache', form.Value, 'cache_max_ttl', _('最大 TTL（秒）'));
     o.datatype = 'uinteger'; o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'cache', 'max_ttl'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'cache', 'max_ttl', val); };
 
     o = s.taboption('cache', form.Value, 'optimistic_answer_ttl',
-      _('Optimistic TTL (seconds)'),
-      _('TTL for expired entries served from optimistic cache. Default: 30s'));
+      _('乐观 TTL（秒）'),
+      _('从乐观缓存提供的过期条目 TTL。默认：30 秒'));
     o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'cache', 'optimistic_answer_ttl'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'cache', 'optimistic_answer_ttl', val); };
 
-    o = s.taboption('cache', form.Value, 'optimistic_max_age', _('Optimistic cache max age'),
-      _('Entries older than this are removed. Human-readable, e.g. 12h'));
+    o = s.taboption('cache', form.Value, 'optimistic_max_age', _('乐观缓存最大年龄'),
+      _('早于此时间的条目将被移除。人类可读格式，例如 12h'));
     o.placeholder = '12h'; o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'cache', 'optimistic_max_age'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'cache', 'optimistic_max_age', val); };
 
     /* ── TLS ── */
-    o = s.taboption('tls', form.Flag, 'tls_enabled', _('Enable encrypted DNS server'),
-      _('Enables DoT / DoH / DoQ server mode'));
+    o = s.taboption('tls', form.Flag, 'tls_enabled', _('启用加密 DNS 服务器'),
+      _('启用 DoT / DoH / DoQ 服务器模式'));
     o.rmempty = false;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'tls', 'enabled'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'tls', 'enabled', val); };
 
-    o = s.taboption('tls', form.Value, 'tls_crt', _('Certificate file'),
-      _('Path to PEM certificate chain'));
+    o = s.taboption('tls', form.Value, 'tls_crt', _('证书文件'),
+      _('PEM 证书链路径'));
     o.placeholder = '/etc/ssl/certs/server.crt'; o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'tls', 'tls_crt'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'tls', 'tls_crt', val); };
 
-    o = s.taboption('tls', form.Value, 'tls_key', _('Private key file'),
-      _('Path to PEM private key'));
+    o = s.taboption('tls', form.Value, 'tls_key', _('私钥文件'),
+      _('PEM 私钥路径'));
     o.placeholder = '/etc/ssl/private/server.key'; o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'tls', 'tls_key'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'tls', 'tls_key', val); };
 
-    o = s.taboption('tls', form.Value, 'tls_port', _('DoT port (DNS-over-TLS)'));
+    o = s.taboption('tls', form.Value, 'tls_port', _('DoT 端口（DNS-over-TLS）'));
     o.placeholder = '853'; o.datatype = 'port'; o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'tls', 'tls_port'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'tls', 'tls_port', val); };
 
-    o = s.taboption('tls', form.Value, 'https_port', _('DoH port (DNS-over-HTTPS)'));
+    o = s.taboption('tls', form.Value, 'https_port', _('DoH 端口（DNS-over-HTTPS）'));
     o.placeholder = '443'; o.datatype = 'port'; o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'tls', 'https_port'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'tls', 'https_port', val); };
 
-    o = s.taboption('tls', form.Value, 'quic_port', _('DoQ port (DNS-over-QUIC)'));
+    o = s.taboption('tls', form.Value, 'quic_port', _('DoQ 端口（DNS-over-QUIC）'));
     o.placeholder = '853'; o.datatype = 'port'; o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'tls', 'quic_port'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'tls', 'quic_port', val); };
 
-    o = s.taboption('tls', form.Value, 'https_server_name', _('HTTPS server name'),
-      _('Value of the Server header in DoH responses'));
+    o = s.taboption('tls', form.Value, 'https_server_name', _('HTTPS 服务器名称'),
+      _('DoH 响应中 Server 头的值'));
     o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'tls', 'https_server_name'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'tls', 'https_server_name', val); };
 
-    o = s.taboption('tls', form.Value, 'https_userinfo', _('DoH Basic Auth'),
-      _('Require basic authentication for DoH. Format: user:password'));
+    o = s.taboption('tls', form.Value, 'https_userinfo', _('DoH 基本认证'),
+      _('对 DoH 要求基本认证。格式：user:password'));
     o.rmempty = true; o.password = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'tls', 'https_userinfo'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'tls', 'https_userinfo', val); };
 
-    o = s.taboption('tls', form.ListValue, 'tls_min_version', _('Minimum TLS version'));
-    o.value('', _('default'));
+    o = s.taboption('tls', form.ListValue, 'tls_min_version', _('最小 TLS 版本'));
+    o.value('', _('默认'));
     ['1.0','1.1','1.2','1.3'].forEach(function(v) { o.value(v, 'TLS ' + v); });
     o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'tls', 'tls_min_version'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'tls', 'tls_min_version', val); };
 
-    o = s.taboption('tls', form.ListValue, 'tls_max_version', _('Maximum TLS version'));
-    o.value('', _('default'));
+    o = s.taboption('tls', form.ListValue, 'tls_max_version', _('最大 TLS 版本'));
+    o.value('', _('默认'));
     ['1.0','1.1','1.2','1.3'].forEach(function(v) { o.value(v, 'TLS ' + v); });
     o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'tls', 'tls_max_version'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'tls', 'tls_max_version', val); };
 
     /* ── Privacy & Security ── */
-    o = s.taboption('privacy', form.Flag, 'edns_enabled', _('Enable EDNS Client Subnet'),
-      _('Pass client IP prefix to upstream for geo-aware responses'));
+    o = s.taboption('privacy', form.Flag, 'edns_enabled', _('启用 EDNS 客户端子网'),
+      _('将客户端 IP 前缀传递给上游以获得地理感知响应'));
     o.rmempty = false;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'edns', 'enabled'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'edns', 'enabled', val); };
 
-    o = s.taboption('privacy', form.Value, 'edns_addr', _('EDNS Client Address'),
-      _('Override client IP sent to upstream (useful behind NAT)'));
+    o = s.taboption('privacy', form.Value, 'edns_addr', _('EDNS 客户端地址'),
+      _('覆盖发送到上游的客户端 IP（NAT 后有用）'));
     o.placeholder = '1.2.3.4'; o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'edns', 'edns_addr'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'edns', 'edns_addr', val); };
 
     o = s.taboption('privacy', form.DynamicList, 'bogus_nxdomain',
-      _('Bogus NXDomain IPs / CIDRs'),
-      _('Responses containing these IPs are replaced with NXDOMAIN'));
+      _('虚假 NXDomain IP / CIDR'),
+      _('包含这些 IP 的响应将替换为 NXDOMAIN'));
     o.placeholder = '0.0.0.0'; o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'bogus_nxdomain', 'ip_addr'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'bogus_nxdomain', 'ip_addr', val); };
 
     o = s.taboption('privacy', form.Flag, 'private_rdns_enabled',
-      _('Use private rDNS upstreams'),
-      _('Route PTR/SOA/NS queries for private addresses to local servers'));
+      _('使用私有 rDNS 上游'),
+      _('将私有地址的 PTR/SOA/NS 查询路由到本地服务器'));
     o.rmempty = false;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'private_rdns', 'enabled'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'private_rdns', 'enabled', val); };
 
     o = s.taboption('privacy', form.DynamicList, 'private_rdns_upstream',
-      _('Private rDNS upstreams'), _('Servers for reverse DNS of private addresses'));
+      _('私有 rDNS 上游'), _('用于私有地址反向 DNS 的服务器'));
     o.placeholder = '127.0.0.1:53'; o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'private_rdns', 'upstream'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'private_rdns', 'upstream', val); };
 
-    o = s.taboption('privacy', form.DynamicList, 'private_subnets', _('Private subnets'),
-      _('Subnets considered private for rDNS routing'));
+    o = s.taboption('privacy', form.DynamicList, 'private_subnets', _('私有子网'),
+      _('rDNS 路由中视为私有的子网'));
     o.placeholder = '192.168.0.0/16'; o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'private_rdns', 'private_subnets'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'private_rdns', 'private_subnets', val); };
 
-    o = s.taboption('privacy', form.Flag, 'dns64_enabled', _('Enable DNS64'),
-      _('Synthesize AAAA records for IPv4-only hosts (for NAT64 networks)'));
+    o = s.taboption('privacy', form.Flag, 'dns64_enabled', _('启用 DNS64'),
+      _('为仅 IPv4 主机合成 AAAA 记录（用于 NAT64 网络）'));
     o.rmempty = false;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'dns64', 'enabled'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'dns64', 'enabled', val); };
 
-    o = s.taboption('privacy', form.DynamicList, 'dns64_prefix', _('DNS64 prefixes'),
-      _('Default: 64:ff9b::/96 (Well-Known Prefix)'));
+    o = s.taboption('privacy', form.DynamicList, 'dns64_prefix', _('DNS64 前缀'),
+      _('默认：64:ff9b::/96（Well-Known 前缀）'));
     o.placeholder = '64:ff9b::'; o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'dns64', 'dns64_prefix'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'dns64', 'dns64_prefix', val); };
 
-    o = s.taboption('privacy', form.Flag, 'hosts_enabled', _('Use hosts files'),
-      _('Resolve names using local hosts files'));
+    o = s.taboption('privacy', form.Flag, 'hosts_enabled', _('使用 hosts 文件'),
+      _('使用本地 hosts 文件解析名称'));
     o.rmempty = false;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'hosts', 'enabled'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'hosts', 'enabled', val); };
 
-    o = s.taboption('privacy', form.DynamicList, 'hosts_files', _('Hosts file paths'));
+    o = s.taboption('privacy', form.DynamicList, 'hosts_files', _('hosts 文件路径'));
     o.placeholder = '/etc/hosts'; o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'hosts', 'hosts_files'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'hosts', 'hosts_files', val); };
 
     /* ── Performance ── */
-    o = s.taboption('perf', form.Value, 'timeout', _('Upstream query timeout'),
-      _('Timeout for outbound DNS queries. Human-readable: 10s, 1m'));
+    o = s.taboption('perf', form.Value, 'timeout', _('上游查询超时'),
+      _('出站 DNS 查询超时。人类可读：10s、1m'));
     o.placeholder = '10s'; o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'global', 'timeout'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'global', 'timeout', val); };
 
-    o = s.taboption('perf', form.Value, 'rate_limit', _('Rate limit (requests/sec)'),
-      _('0 = disabled'));
+    o = s.taboption('perf', form.Value, 'rate_limit', _('速率限制（请求/秒）'),
+      _('0 = 禁用'));
     o.datatype = 'uinteger'; o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'global', 'rate_limit'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'global', 'rate_limit', val); };
 
     o = s.taboption('perf', form.Value, 'ratelimit_subnet_len_ipv4',
-      _('Rate limit subnet length IPv4'),
-      _('Clients in the same /N share one rate limit bucket'));
+      _('速率限制子网长度 IPv4'),
+      _('同一 /N 中的客户端共享一个速率限制桶'));
     o.datatype = 'uinteger'; o.placeholder = '24'; o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'global', 'ratelimit_subnet_len_ipv4'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'global', 'ratelimit_subnet_len_ipv4', val); };
 
     o = s.taboption('perf', form.Value, 'ratelimit_subnet_len_ipv6',
-      _('Rate limit subnet length IPv6'));
+      _('速率限制子网长度 IPv6'));
     o.datatype = 'uinteger'; o.placeholder = '56'; o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'global', 'ratelimit_subnet_len_ipv6'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'global', 'ratelimit_subnet_len_ipv6', val); };
 
-    o = s.taboption('perf', form.Value, 'udp_buf_size', _('UDP buffer size (bytes)'),
-      _('0 = system default'));
+    o = s.taboption('perf', form.Value, 'udp_buf_size', _('UDP 缓冲区大小（字节）'),
+      _('0 = 系统默认'));
     o.datatype = 'uinteger'; o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'global', 'udp_buf_size'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'global', 'udp_buf_size', val); };
 
-    o = s.taboption('perf', form.Value, 'max_go_routines', _('Max goroutines'),
-      _('0 = unlimited'));
+    o = s.taboption('perf', form.Value, 'max_go_routines', _('最大 goroutines'),
+      _('0 = 无限制'));
     o.datatype = 'uinteger'; o.rmempty = true;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'global', 'max_go_routines'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'global', 'max_go_routines', val); };
 
-    o = s.taboption('perf', form.Flag, 'refuse_any', _('Refuse ANY requests'),
-      _('Drop DNS queries of type ANY'));
+    o = s.taboption('perf', form.Flag, 'refuse_any', _('拒绝 ANY 请求'),
+      _('丢弃类型为 ANY 的 DNS 查询'));
     o.rmempty = false;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'global', 'refuse_any'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'global', 'refuse_any', val); };
 
     o = s.taboption('perf', form.Flag, 'pending_requests_enabled',
-      _('Deduplicate pending requests'),
-      _('One upstream query for identical concurrent requests. Disabling risks cache poisoning.'));
+      _('去重挂起请求'),
+      _('相同的并发请求只发一次上游查询。禁用有缓存投毒风险。'));
     o.rmempty = false;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'global', 'pending_requests_enabled'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'global', 'pending_requests_enabled', val); };
 
-    o = s.taboption('perf', form.Flag, 'pprof', _('Enable pprof'),
-      _('Expose Go profiling endpoint on localhost:6060'));
+    o = s.taboption('perf', form.Flag, 'pprof', _('启用 pprof'),
+      _('在 localhost:6060 暴露 Go 性能分析端点'));
     o.rmempty = false;
     o.cfgvalue = function() { return uci.get('dnsproxy', 'global', 'pprof'); };
     o.write    = function(sid, val) { uci.set('dnsproxy', 'global', 'pprof', val); };
diff --git a/www/luci-static/resources/view/dnsproxy/diagnostics.js b/www/luci-static/resources/view/dnsproxy/diagnostics.js
index 867032a..661328a 100644
--- a/www/luci-static/resources/view/dnsproxy/diagnostics.js
+++ b/www/luci-static/resources/view/dnsproxy/diagnostics.js
@@ -248,7 +248,7 @@ function buildPort53Block(data) {
             E('div', {}, [
                 E('span', { class: 'ifacebadge' }, 'Port 53'),
                 ' ',
-                _('not in use.'),
+                _('未被占用。'),
             ]),
         )
     } else if (owner === 'dnsmasq') {
@@ -256,7 +256,7 @@ function buildPort53Block(data) {
             E('div', {}, [
                 E('span', { class: 'ifacebadge' }, 'Port 53'),
                 ' ',
-                _('handled by'),
+                _('由以下进程处理'),
                 ' ',
                 E('strong', {}, 'dnsmasq'),
             ]),
@@ -268,7 +268,7 @@ function buildPort53Block(data) {
                     { class: 'alert-message success', style: 'margin-top:6px' },
                     [
                         '✔ ',
-                        _('Forwarding to dnsproxy via '),
+                        _('通过以下方式转发到 dnsproxy：'),
                         E('code', {}, redirect),
                     ],
                 ),
@@ -280,13 +280,13 @@ function buildPort53Block(data) {
                     { class: 'alert-message warning', style: 'margin-top:6px' },
                     [
                         '⚠ ',
-                        _('No forwarding to dnsproxy. See the '),
+                        _('未转发到 dnsproxy。请参阅'),
                         E(
                             'a',
                             { href: L.url('admin/services/dnsproxy/help') },
-                            _('Help page'),
+                            _('帮助页面'),
                         ),
-                        _(' for setup instructions.'),
+                        _(' 获取设置说明。'),
                     ],
                 ),
             )
@@ -295,7 +295,7 @@ function buildPort53Block(data) {
         rows.push(
             E('div', { class: 'alert-message success' }, [
                 '✔ ',
-                _('dnsproxy listens directly on port 53.'),
+                _('dnsproxy 直接监听 53 端口。'),
             ]),
         )
         // dnsmasq status
@@ -304,10 +304,10 @@ function buildPort53Block(data) {
                 E('strong', {}, 'dnsmasq: '),
                 dmRun
                     ? E('span', {}, [
-                          _('running, port(s) '),
+                          _('运行中，端口：'),
                           E('code', {}, dmPorts.join(', ')),
                       ])
-                    : E('span', { style: 'color:#6c757d' }, _('not running')),
+                    : E('span', { style: 'color:#6c757d' }, _('未运行')),
             ]),
         )
     } else {
@@ -316,7 +316,7 @@ function buildPort53Block(data) {
             E('div', {}, [
                 E('span', { class: 'ifacebadge' }, 'Port 53'),
                 ' ',
-                _('in use by an unknown process.'),
+                _('被未知进程占用。'),
             ]),
         )
     }
@@ -335,13 +335,13 @@ function buildPort53Block(data) {
 function buildStatusBlock(data) {
     var sockTable = E('table', { class: 'table' }, [
         E('tr', { class: 'tr table-titles' }, [
-            E('th', { class: 'th' }, _('Local Address')),
-            E('th', { class: 'th' }, _('Ports')),
-            E('th', { class: 'th' }, _('Protocols')),
+            E('th', { class: 'th' }, _('本地地址')),
+            E('th', { class: 'th' }, _('端口')),
+            E('th', { class: 'th' }, _('协议')),
             E(
                 'th',
                 { class: 'th', style: 'width:5em;text-align:center' },
-                _('Sockets'),
+                _('套接字'),
             ),
         ]),
     ])
@@ -371,8 +371,8 @@ function buildStatusBlock(data) {
             'em',
             {},
             data.dnsproxyUid
-                ? _('No listening sockets — service may be stopped.')
-                : _('Cannot determine dnsproxy uid from /etc/passwd.'),
+                ? _('没有监听套接字——服务可能已停止。')
+                : _('无法从 /etc/passwd 确定 dnsproxy 用户 ID。'),
         ),
     )
 
@@ -390,7 +390,7 @@ function buildStatusBlock(data) {
                         {
                             style: 'display:block;margin-bottom:6px;font-size:13px',
                         },
-                        _('Port 53 status'),
+                        _('端口 53 状态'),
                     ),
                     buildPort53Block(data),
                 ]),
@@ -496,7 +496,7 @@ return view.extend({
 
         return E('div', {}, [
             E('div', { class: 'cbi-section' }, [
-                E('h3', {}, _('DNS Proxy — Status')),
+                E('h3', {}, _('DNS 代理 — 状态')),
                 E(
                     'p',
                     { class: 'cbi-section-descr' },
@@ -508,7 +508,7 @@ return view.extend({
             ]),
 
             E('div', { class: 'cbi-section' }, [
-                E('h3', {}, _('DNS Proxy — Diagnostics')),
+                E('h3', {}, _('DNS 代理 — 诊断')),
 
                 E('div', { class: 'cbi-value' }, [
                     E(
@@ -517,7 +517,7 @@ return view.extend({
                             class: 'cbi-value-title',
                             for: 'dnsproxy-server-select',
                         },
-                        _('DNS Server'),
+                        _('DNS 服务器'),
                     ),
                     E('div', { class: 'cbi-value-field' }, [
                         E(
@@ -534,13 +534,13 @@ return view.extend({
                         E(
                             'span',
                             { class: 'cbi-value-description' },
-                            _('Live dnsproxy listeners. Used for DNS Lookup.'),
+                            _('dnsproxy 实时监听器。用于 DNS 查询。'),
                         ),
                     ]),
                 ]),
 
                 E('div', { class: 'cbi-value' }, [
-                    E('label', { class: 'cbi-value-title' }, _('Target Host')),
+                    E('label', { class: 'cbi-value-title' }, _('目标主机')),
                     E('div', { class: 'cbi-value-field' }, [
                         E('input', {
                             id: 'dnsproxy-diag-host',
@@ -560,7 +560,7 @@ return view.extend({
                                         'handleDig',
                                     ),
                                 },
-                                _('DNS Lookup'),
+                                _('DNS 查询'),
                             ),
                             '\u00a0',
                             E(
@@ -584,14 +584,14 @@ return view.extend({
                                         'handleTraceroute',
                                     ),
                                 },
-                                _('Traceroute'),
+                                _('路由追踪'),
                             ),
                         ]),
                     ]),
                 ]),
 
                 E('div', { class: 'cbi-value' }, [
-                    E('label', { class: 'cbi-value-title' }, _('Output')),
+                    E('label', { class: 'cbi-value-title' }, _('输出')),
                     E('div', { class: 'cbi-value-field' }, [
                         E('textarea', {
                             id: 'dnsproxy-diag-output',
diff --git a/www/luci-static/resources/view/dnsproxy/file.js b/www/luci-static/resources/view/dnsproxy/file.js
index 5793d61..478123b 100644
--- a/www/luci-static/resources/view/dnsproxy/file.js
+++ b/www/luci-static/resources/view/dnsproxy/file.js
@@ -13,9 +13,9 @@ return view.extend({
 
   render: function(content) {
     return E('div', { 'class': 'cbi-section' }, [
-      E('h3', _('Configuration File')),
+      E('h3', _('配置文件')),
       E('p', { 'class': 'cbi-section-descr' },
-        _('Edit /etc/config/dnsproxy directly. Single source of truth — saved directly to disk, bypassing UCI.')),
+        _('直接编辑 /etc/config/dnsproxy。这是唯一的配置来源——直接保存到磁盘，绕过 UCI。')),
       E('textarea', {
         'style': 'width:100%;padding:5px;font-family:monospace;margin-top:.4em',
         'spellcheck': 'false',
@@ -29,10 +29,10 @@ return view.extend({
     var value = document.querySelector('textarea').value;
     return fs.write(filePath, value).then(function() {
       ui.addNotification(null,
-        E('p', _('Configuration file saved.')), 'info');
+        E('p', _('配置文件已保存。')), 'info');
     }).catch(function(e) {
       ui.addNotification(null,
-        E('p', _('Unable to save: %s').format(e.message)));
+        E('p', _('无法保存：%s').format(e.message)));
     });
   },
 
diff --git a/www/luci-static/resources/view/dnsproxy/help.js b/www/luci-static/resources/view/dnsproxy/help.js
index fae0d68..c021d12 100644
--- a/www/luci-static/resources/view/dnsproxy/help.js
+++ b/www/luci-static/resources/view/dnsproxy/help.js
@@ -4,7 +4,7 @@
 return view.extend({
     render: function () {
         var help_content = [
-            E('h3', {}, _('DNS Proxy - Help & Documentation')),
+            E('h3', {}, _('DNS 代理 - 帮助与文档')),
             E(
                 'p',
                 { class: 'description' },
@@ -14,7 +14,7 @@ return view.extend({
             ),
 
             E('div', { class: 'alert alert-info' }, [
-                E('p', {}, _('Supported Protocols:')),
+                E('p', {}, _('支持的协议：')),
                 E('div', { style: 'margin-top:5px;' }, [
                     E('span', { class: 'badge' }, 'Plain DNS'),
                     E('span', { class: 'badge badge-info' }, 'DoT'),
@@ -32,7 +32,7 @@ return view.extend({
                         target: '_blank',
                         class: 'btn btn-small',
                     },
-                    '📖 ' + _('Official Documentation'),
+                    '📖 ' + _('官方文档'),
                 ),
                 ' ',
                 E(
@@ -49,7 +49,7 @@ return view.extend({
             E('hr'),
 
             // --- Upstream Server URL Formats ---
-            E('h4', {}, '🌐 ' + _('Upstream Server URL Formats')),
+            E('h4', {}, '🌐 ' + _('上游服务器 URL 格式')),
             E(
                 'p',
                 {},
@@ -59,23 +59,23 @@ return view.extend({
             ),
             E('table', { class: 'table' }, [
                 E('tr', { class: 'tr table-titles' }, [
-                    E('th', { class: 'th' }, _('URL Format')),
-                    E('th', { class: 'th' }, _('Description')),
-                    E('th', { class: 'th' }, _('Example / Notes')),
+                    E('th', { class: 'th' }, _('URL 格式')),
+                    E('th', { class: 'th' }, _('说明')),
+                    E('th', { class: 'th' }, _('示例 / 备注')),
                 ]),
                 E('tr', { class: 'tr' }, [
                     E('td', { class: 'td' }, E('code', {}, '8.8.8.8:53')),
-                    E('td', { class: 'td' }, _('Plain DNS (UDP/TCP auto)')),
+                    E('td', { class: 'td' }, _('明文 DNS（UDP/TCP 自动）')),
                     E(
                         'td',
                         { class: 'td' },
-                        E('em', {}, _('Default, simplest')),
+                        E('em', {}, _('默认、最简单')),
                     ),
                 ]),
                 E('tr', { class: 'tr' }, [
                     E('td', { class: 'td' }, E('code', {}, 'tcp://1.1.1.1')),
-                    E('td', { class: 'td' }, _('Plain DNS over TCP only')),
-                    E('td', { class: 'td' }, _('Forced TCP connection')),
+                    E('td', { class: 'td' }, _('仅 TCP 的明文 DNS')),
+                    E('td', { class: 'td' }, _('强制 TCP 连接')),
                 ]),
                 E('tr', { class: 'tr' }, [
                     E(
@@ -83,8 +83,8 @@ return view.extend({
                         { class: 'td' },
                         E('code', {}, 'tls://dns.adguard.com'),
                     ),
-                    E('td', { class: 'td' }, _('DNS-over-TLS (DoT)')),
-                    E('td', { class: 'td' }, _('Encrypted, default port 853')),
+                    E('td', { class: 'td' }, _('DNS-over-TLS（DoT）')),
+                    E('td', { class: 'td' }, _('加密，默认端口 853')),
                 ]),
                 E('tr', { class: 'tr' }, [
                     E(
@@ -92,8 +92,8 @@ return view.extend({
                         { class: 'td' },
                         E('code', {}, 'https://dns.adguard.com/dns-query'),
                     ),
-                    E('td', { class: 'td' }, _('DNS-over-HTTPS (DoH)')),
-                    E('td', { class: 'td' }, _('Encrypted, default port 443')),
+                    E('td', { class: 'td' }, _('DNS-over-HTTPS（DoH）')),
+                    E('td', { class: 'td' }, _('加密，默认端口 443')),
                 ]),
                 E('tr', { class: 'tr' }, [
                     E(
@@ -101,18 +101,18 @@ return view.extend({
                         { class: 'td' },
                         E('code', {}, 'quic://dns.adguard.com'),
                     ),
-                    E('td', { class: 'td' }, _('DNS-over-QUIC (DoQ)')),
-                    E('td', { class: 'td' }, _('Fastest encrypted protocol')),
+                    E('td', { class: 'td' }, _('DNS-over-QUIC（DoQ）')),
+                    E('td', { class: 'td' }, _('最快的加密协议')),
                 ]),
                 E('tr', { class: 'tr' }, [
                     E('td', { class: 'td' }, E('code', {}, 'sdns://...')),
                     E('td', { class: 'td' }, _('DNSCrypt / DNS Stamp')),
-                    E('td', { class: 'td' }, _('Full stamp string required')),
+                    E('td', { class: 'td' }, _('需要完整 stamp 字符串')),
                 ]),
             ]),
 
             // --- Domain-Specific Upstreams ---
-            E('h4', {}, '🎯 ' + _('Domain-Specific Upstreams')),
+            E('h4', {}, '🎯 ' + _('按域指定的上游')),
             E(
                 'p',
                 {},
@@ -123,8 +123,8 @@ return view.extend({
             ),
             E('table', { class: 'table' }, [
                 E('tr', { class: 'tr table-titles' }, [
-                    E('th', { class: 'th' }, _('Pattern')),
-                    E('th', { class: 'th' }, _('Behavior')),
+                    E('th', { class: 'th' }, _('匹配模式')),
+                    E('th', { class: 'th' }, _('行为')),
                 ]),
                 E('tr', { class: 'tr' }, [
                     E(
@@ -135,7 +135,7 @@ return view.extend({
                     E(
                         'td',
                         { class: 'td' },
-                        _('All *.local domains → local DNS server'),
+                        _('所有 *.local 域 → 本地 DNS 服务器'),
                     ),
                 ]),
                 E('tr', { class: 'tr' }, [
@@ -147,7 +147,7 @@ return view.extend({
                     E(
                         'td',
                         { class: 'td' },
-                        _('Subdomain → internal corporate DNS'),
+                        _('子域名 → 内部企业 DNS'),
                     ),
                 ]),
                 E('tr', { class: 'tr' }, [
@@ -155,7 +155,7 @@ return view.extend({
                     E(
                         'td',
                         { class: 'td' },
-                        _('Unqualified (single-label) names'),
+                        _('非限定（单标签）名称'),
                     ),
                 ]),
                 E('tr', { class: 'tr' }, [
@@ -167,29 +167,29 @@ return view.extend({
                     E(
                         'td',
                         { class: 'td' },
-                        _('Specific domain with encrypted DNS'),
+                        _('使用加密 DNS 的特定域'),
                     ),
                 ]),
             ]),
 
             // --- Upstream Modes ---
-            E('h4', {}, '⚙️ ' + _('Upstream Modes')),
+            E('h4', {}, '⚙️ ' + _('上游模式')),
             E(
                 'p',
                 {},
-                _('Control how multiple upstream servers are utilized:'),
+                _('控制多个上游服务器的使用方式：'),
             ),
             E('table', { class: 'table' }, [
                 E('tr', { class: 'tr table-titles' }, [
-                    E('th', { class: 'th' }, _('Mode')),
-                    E('th', { class: 'th' }, _('Description')),
+                    E('th', { class: 'th' }, _('模式')),
+                    E('th', { class: 'th' }, _('说明')),
                 ]),
                 E('tr', { class: 'tr' }, [
                     E('td', { class: 'td' }, E('code', {}, 'load_balance')),
                     E(
                         'td',
                         { class: 'td' },
-                        _('Distribute queries across all upstreams evenly'),
+                        _('将查询均匀分布到所有上游'),
                     ),
                 ]),
                 E('tr', { class: 'tr' }, [
@@ -215,14 +215,14 @@ return view.extend({
             ]),
 
             // --- Popular Public DNS ---
-            E('h4', {}, '🏢 ' + _('Popular Public DNS Servers')),
-            E('p', {}, _('Ready-to-use public DNS resolvers:')),
+            E('h4', {}, '🏢 ' + _('常用公共 DNS 服务器')),
+            E('p', {}, _('可直接使用的公共 DNS 解析器：')),
             E('table', { class: 'table' }, [
                 E('tr', { class: 'tr table-titles' }, [
-                    E('th', { class: 'th' }, _('Provider')),
-                    E('th', { class: 'th' }, _('DoT Upstream')),
-                    E('th', { class: 'th' }, _('DoH Upstream')),
-                    E('th', { class: 'th' }, _('Notes')),
+                    E('th', { class: 'th' }, _('提供商')),
+                    E('th', { class: 'th' }, _('DoT 上游')),
+                    E('th', { class: 'th' }, _('DoH 上游')),
+                    E('th', { class: 'th' }, _('备注')),
                 ]),
                 E('tr', { class: 'tr' }, [
                     E('td', { class: 'td' }, E('strong', {}, 'Cloudflare')),
@@ -232,7 +232,7 @@ return view.extend({
                         { class: 'td' },
                         E('code', {}, 'https://1.1.1.1/dns-query'),
                     ),
-                    E('td', { class: 'td' }, _('Fast, privacy-focused')),
+                    E('td', { class: 'td' }, _('快速、注重隐私')),
                 ]),
                 E('tr', { class: 'tr' }, [
                     E('td', { class: 'td' }, E('strong', {}, 'Google')),
@@ -245,7 +245,7 @@ return view.extend({
                     E(
                         'td',
                         { class: 'td' },
-                        _('Reliable, global infrastructure'),
+                        _('可靠、全球基础设施'),
                     ),
                 ]),
                 E('tr', { class: 'tr' }, [
@@ -259,7 +259,7 @@ return view.extend({
                     E(
                         'td',
                         { class: 'td' },
-                        _('Security focused, malware blocking'),
+                        _('注重安全、恶意软件拦截'),
                     ),
                 ]),
                 E('tr', { class: 'tr' }, [
@@ -274,7 +274,7 @@ return view.extend({
                         { class: 'td' },
                         E('code', {}, 'https://dns.adguard.com/dns-query'),
                     ),
-                    E('td', { class: 'td' }, _('Built-in ad-blocking filters')),
+                    E('td', { class: 'td' }, _('内置广告拦截过滤器')),
                 ]),
                 E('tr', { class: 'tr' }, [
                     E('td', { class: 'td' }, E('strong', {}, 'NextDNS')),
@@ -291,13 +291,13 @@ return view.extend({
                     E(
                         'td',
                         { class: 'td' },
-                        _('Customizable, requires Account ID'),
+                        _('可自定义，需要账户 ID'),
                     ),
                 ]),
             ]),
 
             // --- Security & Features ---
-            E('h4', {}, '🔒 ' + _('Security & Privacy Features')),
+            E('h4', {}, '🔒 ' + _('安全与隐私功能')),
             E('ul', {}, [
                 E(
                     'li',
@@ -313,13 +313,13 @@ return view.extend({
                         'Bogus NXDomain - Convert specific responses to NXDomain',
                     ),
                 ),
-                E('li', {}, _('DNS64 - IPv6 translation for NAT64 networks')),
-                E('li', {}, _('Hosts files - Local domain overrides')),
-                E('li', {}, _('Request blocking via blocklists')),
-                E('li', {}, _('DNSSEC validation support')),
+                E('li', {}, _('DNS64 - 为 NAT64 网络提供 IPv6 转换')),
+                E('li', {}, _('hosts 文件 - 本地域覆盖')),
+                E('li', {}, _('通过黑名单拦截请求')),
+                E('li', {}, _('DNSSEC 验证支持')),
             ]),
 
-            E('h4', {}, '💾 ' + _('Caching & Performance')),
+            E('h4', {}, '💾 ' + _('缓存与性能')),
             E('ul', {}, [
                 E(
                     'li',
@@ -328,7 +328,7 @@ return view.extend({
                         'Optimistic cache - Return cached entries while refreshing in background',
                     ),
                 ),
-                E('li', {}, _('Configurable TTL - Control cache expiration')),
+                E('li', {}, _('可配置 TTL - 控制缓存过期')),
                 E(
                     'li',
                     {},
@@ -339,44 +339,44 @@ return view.extend({
                 E(
                     'li',
                     {},
-                    _('Connection pooling - Reuse connections to upstreams'),
+                    _('连接池 - 复用上游连接'),
                 ),
             ]),
 
-            E('h4', {}, '🛡️ ' + _('Server Mode Configuration')),
-            E('p', {}, _('Run dnsproxy as a secure DNS server:')),
+            E('h4', {}, '🛡️ ' + _('服务器模式配置')),
+            E('p', {}, _('将 dnsproxy 作为安全 DNS 服务器运行：')),
             E('ul', {}, [
-                E('li', {}, _('DoT server: tls://0.0.0.0:853')),
-                E('li', {}, _('DoH server: https://0.0.0.0:443')),
-                E('li', {}, _('DoQ server: quic://0.0.0.0:853')),
+                E('li', {}, _('DoT 服务器：tls://0.0.0.0:853')),
+                E('li', {}, _('DoH 服务器：https://0.0.0.0:443')),
+                E('li', {}, _('DoQ 服务器：quic://0.0.0.0:853')),
                 E(
                     'li',
                     {},
-                    _('TLS certificates required for encrypted protocols'),
+                    _('加密协议需要 TLS 证书'),
                 ),
-                E('li', {}, _('Optional authentication for private access')),
+                E('li', {}, _('可选认证以进行私有访问')),
             ]),
 
-            E('h4', {}, '🔧 ' + _('Troubleshooting Tips')),
+            E('h4', {}, '🔧 ' + _('故障排除提示')),
             E('ul', {}, [
-                E('li', {}, _('Check service status in Status → Diagnostics')),
-                E('li', {}, _('View logs in System → Logread')),
+                E('li', {}, _('在 状态 → 诊断 中检查服务状态')),
+                E('li', {}, _('在 系统 → 系统日志 中查看日志')),
                 E(
                     'li',
                     {},
-                    _('Verify upstream connectivity with dig/nslookup'),
+                    _('使用 dig/nslookup 验证上游连通性'),
                 ),
                 E(
                     'li',
                     {},
-                    _('Ensure correct system time for TLS/HTTPS validation'),
+                    _('确保系统时间正确以进行 TLS/HTTPS 校验'),
                 ),
                 E(
                     'li',
                     {},
-                    _('Check firewall rules for DNS ports (53, 853, 443)'),
+                    _('检查 DNS 端口（53、853、443）的防火墙规则'),
                 ),
-                E('li', {}, _('Test with multiple upstreams for redundancy')),
+                E('li', {}, _('使用多个上游进行冗余测试')),
             ]),
 
             E('div', { class: 'alert alert-success' }, [
diff --git a/www/luci-static/resources/view/dnsproxy/logread.js b/www/luci-static/resources/view/dnsproxy/logread.js
index 3b086af..8ccc529 100644
--- a/www/luci-static/resources/view/dnsproxy/logread.js
+++ b/www/luci-static/resources/view/dnsproxy/logread.js
@@ -318,7 +318,7 @@ function fillLog(container, text, levelFilter, openKeys) {
             E(
                 'div',
                 { style: 'color:#5a6473;padding:20px;text-align:center' },
-                _('No messages.') +
+                _('暂无消息。') +
                     (levelFilter !== 'ALL'
                         ? ' (filter: ' + levelFilter + ')'
                         : ''),
@@ -436,7 +436,7 @@ return view.extend({
                 self._lastRaw = log
                 self._rebuild()
                 self._setStatus(
-                    _('updated') + ' ' + new Date().toLocaleTimeString(),
+                    _('已更新') + ' ' + new Date().toLocaleTimeString(),
                 )
                 self._scheduleRefresh()
             })
@@ -476,7 +476,7 @@ return view.extend({
                         self._rebuild()
                     },
                 },
-                lvl === 'ALL' ? _('All') : lvl,
+                lvl === 'ALL' ? _('全部') : lvl,
             )
         })
 
@@ -487,14 +487,14 @@ return view.extend({
                 click: function () {
                     self._paused = !self._paused
                     pauseBtn.textContent = self._paused
-                        ? '▶ ' + _('Resume')
-                        : '⏸ ' + _('Pause')
+                        ? '▶ ' + _('继续')
+                        : '⏸ ' + _('暂停')
                     pauseBtn.className =
                         'dpl-ctrl-btn' + (self._paused ? ' paused' : '')
                     if (!self._paused) self._scheduleRefresh()
                 },
             },
-            '⏸ ' + _('Pause'),
+            '⏸ ' + _('暂停'),
         )
 
         var followBtn = E(
@@ -508,7 +508,7 @@ return view.extend({
                     followBtn.style.display = 'none'
                 },
             },
-            '↓ ' + _('Follow'),
+            '↓ ' + _('跟随'),
         )
         self._followBtn = followBtn
 
diff --git a/www/luci-static/resources/view/dnsproxy/main.js b/www/luci-static/resources/view/dnsproxy/main.js
index 9a30ccc..289002b 100644
--- a/www/luci-static/resources/view/dnsproxy/main.js
+++ b/www/luci-static/resources/view/dnsproxy/main.js
@@ -76,9 +76,9 @@ function updateStatusUI(running) {
 
     if (dot) dot.textContent = getStatusEmoji(enabled, running) + ' '
     if (txt) {
-        if (running) txt.textContent = _('Running')
-        else if (enabled) txt.textContent = _('Stopped (enabled)')
-        else txt.textContent = _('Disabled')
+        if (running) txt.textContent = _('运行中')
+        else if (enabled) txt.textContent = _('已启用但未运行')
+        else txt.textContent = _('已禁用')
     }
 
     if (btnEnable) btnEnable.disabled = enabled
@@ -97,7 +97,7 @@ function makeActionHandler(action) {
             var dot = document.getElementById('dnsproxy-status-dot')
             var txt = document.getElementById('dnsproxy-status-txt')
             if (dot) dot.textContent = '\u26AB ' // ⚫
-            if (txt) txt.textContent = _('Restarting...')
+            if (txt) txt.textContent = _('重启中...')
 
             return callInitAction('dnsproxy', 'restart').catch(function (e) {
                 ui.addNotification(null, E('p', e.message))
@@ -184,12 +184,12 @@ function renderStatus(initStatus, version) {
         ? '\uD83D\uDD34 ' // 🔴 не установлен
         : getStatusEmoji(enabled, running) + ' '
     var initialText = notInstalled
-        ? _('Not installed')
+        ? _('未安装')
         : running
-          ? _('Running')
+          ? _('运行中')
           : enabled
-            ? _('Stopped (enabled)')
-            : _('Disabled')
+            ? _('已启用但未运行')
+            : _('已禁用')
 
     // Поллинг каждые 5 секунд — только если пакет установлен
     if (!notInstalled) {
@@ -206,11 +206,11 @@ function renderStatus(initStatus, version) {
     var longGap = '\u00a0\u00a0\u00a0\u00a0\u00a0\u00a0\u00a0\u00a0'
 
     return E('div', { class: 'cbi-section' }, [
-        E('h3', {}, _('Service Status')),
+        E('h3', {}, _('服务状态')),
 
         // Строка: Service Status
         E('div', { class: 'cbi-value' }, [
-            E('label', { class: 'cbi-value-title' }, _('Service Status')),
+            E('label', { class: 'cbi-value-title' }, _('服务状态')),
             E(
                 'div',
                 {
@@ -226,7 +226,7 @@ function renderStatus(initStatus, version) {
 
         // Строка: Version
         E('div', { class: 'cbi-value' }, [
-            E('label', { class: 'cbi-value-title' }, _('Version')),
+            E('label', { class: 'cbi-value-title' }, _('版本')),
             E(
                 'div',
                 {
@@ -239,7 +239,7 @@ function renderStatus(initStatus, version) {
 
         // Строка: Service Control — 5 кнопок
         E('div', { class: 'cbi-value' }, [
-            E('label', { class: 'cbi-value-title' }, _('Service Control')),
+            E('label', { class: 'cbi-value-title' }, _('服务控制')),
             E('div', { class: 'cbi-value-field' }, [
                 E(
                     'button',
@@ -249,7 +249,7 @@ function renderStatus(initStatus, version) {
                         disabled: !enabled || running || notInstalled || null,
                         click: makeActionHandler('start'),
                     },
-                    _('Start'),
+                    _('启动'),
                 ),
                 gap,
                 E(
@@ -260,7 +260,7 @@ function renderStatus(initStatus, version) {
                         disabled: !running || notInstalled || null,
                         click: makeActionHandler('restart'),
                     },
-                    _('Restart'),
+                    _('重启'),
                 ),
                 gap,
                 E(
@@ -271,7 +271,7 @@ function renderStatus(initStatus, version) {
                         disabled: !running || notInstalled || null,
                         click: makeActionHandler('stop'),
                     },
-                    _('Stop'),
+                    _('停止'),
                 ),
                 longGap,
                 E(
@@ -282,7 +282,7 @@ function renderStatus(initStatus, version) {
                         disabled: enabled || notInstalled || null,
                         click: makeActionHandler('enable'),
                     },
-                    _('Enable'),
+                    _('启用'),
                 ),
                 gap,
                 E(
@@ -293,7 +293,7 @@ function renderStatus(initStatus, version) {
                         disabled: !enabled || notInstalled || null,
                         click: makeActionHandler('disable'),
                     },
-                    _('Disable'),
+                    _('禁用'),
                 ),
             ]),
         ]),
@@ -307,7 +307,7 @@ function renderStatus(initStatus, version) {
                 E(
                     'div',
                     { class: 'cbi-value-description' },
-                    _('Please support the development of this project'),
+                    _('请支持本项目的开发'),
                 ),
             ),
         ]),
@@ -347,7 +347,7 @@ return view.extend({
             Promise.resolve(renderStatus(initStatus, version)),
             m.render(),
         ]).then(function (nodes) {
-            return [nodes[0], E('h3', {}, _('Configuration')), nodes[1]]
+            return [nodes[0], E('h3', {}, _('配置')), nodes[1]]
         })
     },
 
@@ -363,8 +363,8 @@ return view.extend({
                 ui.changes.apply(mode == '0')
             })
             .then(function () {
-                // ALL_DOH patch: 保存后触发 dnsproxy 服务 reload，重新评估 nftables
-                // 拦截规则（ON → 写入 /etc/nftables.d 拦截链并 fw4 reload；OFF → 删除并 fw4 reload）
+                // ALL_DOH patch: 保存后触发 dnsproxy 服务 reload，重新评估 LAN DNS 拦截规则
+                // （fw3 用 iptables / fw4 用 nftables，由 init 自动选择并触发防火墙重载）
                 return callInitAction('dnsproxy', 'reload').catch(function (e) {
                     ui.addNotification(null, E('p', 'DNS 拦截规则更新失败: ' + e.message))
                 })
